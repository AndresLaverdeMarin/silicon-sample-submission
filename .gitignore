# self-check report (regenerated by `make check`)
*_check_report.txt

# maintainer/owner tooling — kept locally, never shipped to participating teams
maintainer/

.dryrun/

# R / Python / editor / OS cruft
.Rhistory
.RData
.Rproj.user/
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/

# --- sim/ Tier-1 pipeline -------------------------------------------------
# The uv environment. Rebuild it with `uv sync`.
.venv/

# The pool is REBUILT by stage 1 from population/, never committed.
sim/out/00_pool/

# Stage-3 raw generations. Large, and deposited to Zenodo separately, not to
# git — see registration.md item K.2. The small outputs in sim/out/ ARE
# tracked, because they are the reproducibility record.
sim/out/*.jsonl
sim/out/*.parquet
